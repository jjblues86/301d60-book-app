<!DOCTYPE html>
<html lang="en">
<head>
  <title>Books</title>
</head>
<body>
  <header>
    <h2>Saved <%= books.length %> Books</h2>
  </header>
  <main>
    <section class="books">
      <% if(books) { %>
        <% books.forEach( book => { %>
          <%- include('./books/detail', {show:false, book:book} ) %>
        <% }); %>
      <% } else { %>
        <div class="flex-column">
          <h3> No books with <%= searchType %> <%= searchCriteria %> found. Please refine search criteria and <a href="./">try again</a><h3>
        </div>
      <% }; %>
    </section>
  </main>
  <footer>

  </footer>
</body>
</html>